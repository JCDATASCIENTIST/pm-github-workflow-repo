# PM GitHub Workflow .gitignore

# SECRETS
.env
.env.*
*.key
*.pem
api-keys.md
secrets/

# OS FILES
.DS_Store
Thumbs.db
*.swp
*~

# NODE / NPM
node_modules/
package-lock.json
yarn.lock
bun.lockb

# PYTHON
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# LARGE DATA FILES
*.csv
*.xlsx
*.xls
data/raw/
exports/
recordings/
transcripts/raw/
*.mp3
*.mp4
*.wav
*.m4a
*.mov
*.zip
*.tar.gz

# CLAUDE CODE
.claude/settings.local.json
.claude/statsig/
# Activated skill copies. The source of truth is skills/; .claude/skills/ is a
# local convenience copy (see README), so it shouldn't be committed twice.
.claude/skills/

# IDE FILES
.vscode/
.cursor/
.idea/
*.code-workspace

# BUILD ARTIFACTS
dist/
build/
out/
*.log
.next/
.vercel/

# PM-SPECIFIC
*.otter
*.granola
drafts/wip/
scratch/
screenshots/
screen-recordings/

# RESEARCH OUTPUTS (often contain customer PII / verbatim quotes)
# The skills here save dated files like feedback-synthesis-2026-06-01.md and
# competitor-acme-2026-06-01.md. Patterns are anchored to the DATED form so they
# only catch generated outputs, never the repo's own source (skills/, evals/).
# Ignored by default so a transcript full of customer names can't be committed
# by accident. Want to share a sanitized one? `git add -f <file>`
feedback-synthesis*.md
competitor-*.md
transcripts/
interviews/
data/exports/
# Never ignore the repo's own versioned source. These negations come AFTER the
# patterns above, so a dated OR undated output named like the globs is ignored
# at the repo root but a file under skills/ or evals/ is always committable.
!skills/**
!evals/**

# ENTERPRISE NOTE
# Add regulated document types here
# Consider CODEOWNERS for review requirements
# NDA materials should NEVER be in any repo
